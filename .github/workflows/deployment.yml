name: Deploy react
on: push
jobs:
  test:
      runs-on: ubuntu-latest
      steps:
        - name: Get repos code
          uses: actions/checkout@v6
        - name: Install depend
          run: npm install
        - name: Test app
          run: npm run test
  lint:
      runs-on: ubuntu-latest
      steps:
        - name: Get repos code
          uses: actions/checkout@v6
        - name: Install depend
          run: npm install
        - name: lint app
          run: npm run lint
  build:
      runs-on: ubuntu-latest
      steps:
        - name: Get repos code
          uses: actions/checkout@v6
        - name: Install depend
          run: npm install
        - name: build app
          run: npm run build
        - name: Deploy
          run: echo Deloy.....
