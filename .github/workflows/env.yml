name: Enviroment
on: 
  push:
  workflow_dispatch:
env:
  NODE_ENV: production
  GH_SECRET: 42 
jobs:
  bulid:
    runs-on: ubuntu-latest
    steps:
      - name: print env bulid
        run: |
          echo "${{ env.NODE_ENV }}"
          echo "${{ env.GH_SECRET }}"
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: print env deploy
        run: |
          echo "${{ env.NODE_ENV }}"
          echo "${{ env.GH_SECRET }}"