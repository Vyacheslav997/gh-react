name: Deploy react
on: push
jobs:
  deploy:
      runs-on: ubuntu-latest
      steps:
        - name: Get repos code
          uses: actions/checkout@v6
        - name: Install depend
          run: npm install
        - name: Test app
          run: npm run test
        - name: lint app
          run: npm run lint
        - name: build app
          run: npm run build
        - name: Deploy
          run: echo Deloy.....
